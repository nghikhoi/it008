<Window x:Class="UI.VideoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="VideoWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="127*"></RowDefinition>
            <RowDefinition Height="13*" ></RowDefinition>
        </Grid.RowDefinitions>
        <MediaElement Grid.Row="0"
            ScrubbingEnabled="True"
            Name="VideoControl" LoadedBehavior="Manual" UnloadedBehavior="Stop" Margin="0,0,-0.4,5.8" Loaded="Loaded_acitivity" >

        </MediaElement>



        <Button
                Grid.Row="1"
                Height="23" HorizontalAlignment="Left" Margin="15,0,0,13"

            Name= "PlayButton" VerticalAlignment="Bottom" Width="75" Click="play_video_on_click" >

            Play
        </Button>

        <Button
                    Grid.Row="1"
                    Height="23" HorizontalAlignment="Left" Margin="103,0,0,13"

            Name="PauseButton" VerticalAlignment="Bottom" Width="75" Click="pause_video_on_click"

           >

            Pause
        </Button>
    </Grid>
</Window>
